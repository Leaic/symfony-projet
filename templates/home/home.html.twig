{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}


<div class="jumbotron text-center">
    <h1>Agence Leaic</h1>
    <p>kjnjjjij jiujjmo imjokoijihu uidjioe ddzaodue dzaùazdaudscn 
    dkjazndiojzad zdbzaiudjzkdmahcdcdbcdjjjjjjùidjlqdjf naùeuedjhe
    </p>

    <a href="{{ path ('property.index')}}" class=" btn btn-danger text-center">Achat</a>

</div>

<div class="container">
    <h2>Les derniers biens</h2>
    <div class="row">
        {% for property in properties %}
            <div class="row ">
                <div class="card col-md-6">
                    <div class="card-body">
                        <h5 class="card.title">
                            <a href="{{ path ('property.show', {id: property.id}) }}" title="cliquer pour en savoir plus sur le bien">{{property.title}} </a>
                        </h5>
                        <p class="card-text">{{ property.city }} ({{ property.postalCode }})</p>
                        <div class="text-primary" style="font-weight: bold; font-size: 2rem;">{{ property.formattedPrice }} £</div>

                        
                        <!--<a href="{#}{{ path ('property.index')}}"{#}#" class=" btn btn-danger text-center">Acheter</a>-->

                    </div>

                </div>
            <div>
         {% endfor %} 
    </div>
</div>
{% endblock %}
